/* esperamos a que la pagina este cargada */
document.addEventListener("DOMContentLoaded", function(){

    let button = document.getElementById("mybutton");
    /* printeamos el elemento en la console de la pagina*/
    console.log(button);
   
});
